#include <avr/io.h>
#include <util/delay.h>

int main(void)
{
    DDRB |= (1 << PB0);   // Set PB0 as output

    while (1)
    {
        PORTB |= (1 << PB0);   // LED ON
        _delay_ms(500);

        PORTB &= ~(1 << PB0);  // LED OFF
        _delay_ms(500);
    }
}
